import 'package:flutter/material.dart';

AppBar mainAppBar({@required String appBarTitle}){

  return AppBar(
    title: Text(appBarTitle),
    actions: <Widget>[

    ],
    centerTitle: true,
  );

}
AppBar newAppBar({@required String appBarTitle}){

  return AppBar(
    title: Text(appBarTitle),
    actions: <Widget>[

    ],
    centerTitle: true,
  );

}